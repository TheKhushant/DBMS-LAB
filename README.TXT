DDL = data defination language
  |--> decimal (5,2)
  |--> integer 
  |--> char (fixed length
  |--> varchar (not fixed , required space )
  |--> Date 

create table _name of table_ ;

             colurm == attribute, row == tuple
